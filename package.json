{
  "name": "everything-use-codex",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@10.22.0",
  "scripts": {
    "build": "pnpm --filter './packages/**' build",
    "typecheck": "pnpm --filter './packages/**' typecheck",
    "test": "pnpm --filter './packages/**' test",
    "check": "pnpm typecheck && pnpm test && pnpm build"
  }
}

